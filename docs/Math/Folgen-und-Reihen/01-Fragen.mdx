---
page_id: f056e551-a6ed-40be-80c7-8cc8b49c0f37
---

import ChoiceAnswer from '@tdev-components/documents/Assessable/ChoiceAnswer';
import TrueFalseAnswer from '@tdev-components/documents/Assessable/TrueFalseAnswer';
import Quiz from '@tdev-components/documents/Assessable/Quiz';
import { points, multipleChoicePoints, noPoints } from '@tdev-components/documents/Assessable/helpers/scoring';

# Fragen Folgen und Reihen

<Quiz scoring={points(1)} randomizeQuestions randomizeOptions resetMode='incorrect' id="8fb8300e-f5c2-473e-ba74-91d0c6c0b0a8" >
    <ChoiceAnswer correct={[1, 3]} scoring={multipleChoicePoints(2, 1)} multiple>
        > Welche Aussagen zur arithmetischen Folge treffen zu?

        1. Die Differenz zweier aufeinanderfolgender Glieder ist konstant.
        2. Der Quotient zweier aufeinanderfolgender Glieder ist konstant.
        3. Das n-te Glied lautet $a_n = a_1 + (n-1) \cdot d$.
        4. Sie wächst stets exponentiell.
    </ChoiceAnswer>

    <ChoiceAnswer correct={[1]} scoring={points(1)}>
        > Eine arithmetische Folge hat $a_1 = 5$ und $d = 3$. Wie lautet $a_{10}$?

        1. 32
        2. 29
        3. 35
        4. 27
    </ChoiceAnswer>

    <ChoiceAnswer correct={[1, 2, 4]} scoring={multipleChoicePoints(2, 1)} multiple>
        > Welche Aussagen zur geometrischen Folge treffen zu?

        1. Der Quotient zweier aufeinanderfolgender Glieder ist konstant.
        2. Das n-te Glied lautet $a_n = a_1 \cdot q^{n-1}$.
        3. Sie kann nur positive Glieder besitzen.
        4. Bei $q = 1$ ist die Folge konstant.
    </ChoiceAnswer>

    <ChoiceAnswer correct={[1]} scoring={points(1)}>
        > Eine geometrische Folge hat $a_1 = 3$ und $q = 2$. Wie lautet $a_6$?

        1. 96
        2. 48
        3. 192
        4. 64
    </ChoiceAnswer>

    <ChoiceAnswer correct={[1]} scoring={points(1)}>
        > Wie lautet die Formel für die Summe der ersten $n$ Glieder einer arithmetischen Folge?

        1. $s_n = \dfrac{n}{2} \cdot (a_1 + a_n)$
        2. $s_n = a_1 \cdot \dfrac{q^n - 1}{q - 1}$
        3. $s_n = n \cdot a_1 + d$
        4. $s_n = \dfrac{a_1}{1 - q}$
    </ChoiceAnswer>

    <ChoiceAnswer correct={[1]} scoring={points(1)}>
        > Eine arithmetische Folge hat $a_1 = 2$ und $d = 5$. Wie gross ist $s_{20}$ (Summe der ersten 20 Glieder)?

        1. 990
        2. 950
        3. 1000
        4. 980
    </ChoiceAnswer>

    <ChoiceAnswer correct={[1]} scoring={points(1)}>
        > Wie lautet die Formel für die Summe der ersten $n$ Glieder einer geometrischen Folge (mit $q \neq 1$)?

        1. $s_n = a_1 \cdot \dfrac{q^n - 1}{q - 1}$
        2. $s_n = \dfrac{n}{2} \cdot (a_1 + a_n)$
        3. $s_n = \dfrac{a_1}{1 - q}$
        4. $s_n = a_1 + (n-1) \cdot d$
    </ChoiceAnswer>

    <ChoiceAnswer correct={[4]} scoring={points(1)}>
        > Eine geometrische Folge hat $a_n = 2 \cdot 0.5^{n-1}$. Wie gross ist $s_{10}$ (Summe der ersten 10 Glieder)?

        1. 3.69609375
        2. 3.79609375
        3. 3.89609375
        4. 3.99609375
    </ChoiceAnswer>

    <ChoiceAnswer correct={[1]} scoring={points(1)}>
        > Betrachte die Folge $a_n = \dfrac{2n+1}{n+3}$. Welche Aussage ist richtig?

        1. Die Folge konvergiert gegen den Grenzwert 2.
        2. Die Folge ist divergent.
        3. Der Grenzwert lautet $\dfrac{1}{3}$.
        4. Die Folge ist für $n \geq 1$ streng monoton fallend.
    </ChoiceAnswer>
</Quiz>

## Zusatzfrage

<ChoiceAnswer correct={[1, 2, 4]} scoring={multipleChoicePoints(2, 1)} multiple id="ffd0cb6d-e72f-4455-9c4f-246a21cbbbe6">
    > Welche Aussagen zur unendlichen geometrischen Reihe treffen zu?

    1. Sie konvergiert genau dann, wenn $|q| < 1$.
    2. Für $|q| < 1$ gilt $S = \dfrac{a_1}{1 - q}$.
    3. Sie konvergiert für alle Werte von $q$.
    4. Für $q = 1$ divergiert die Reihe (sofern $a_1 \neq 0$).
</ChoiceAnswer>