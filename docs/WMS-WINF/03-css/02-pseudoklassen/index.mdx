---
page_id: 8198328b-5d4d-4fdf-94d7-694df190bd77
---

import HtmlEditor from '@tdev-components/documents/CodeEditor/HtmlEditor';
import ProgressState from '@tdev-components/documents/ProgressState';
import CodeBlock from '@theme/CodeBlock';
import GridDemo from './assets/GridDemo';
import GridSkeleton from '!!raw-loader!./assets/grid-skeleton.html';
import Grid from '!!raw-loader!./assets/grid.html';
import CvHtml from '!!raw-loader!../01-tag-selektoren/assets/cv.html';

# Pseudoklassen

Über sogenannte __Pseudoklassen__ können CSS-Regeln definiert werden, die auf bestimmte Zustände von HTML-Elementen reagieren.

Beispielsweise wenn ein Element besonders formatiert werden soll, wenn es mit der Maus berührt wird (`:hover`), oder wenn ein Element das erste Element einer Liste ist (`:first-child`).

Diese Pseudoklassen starten immer mit einem Doppelpunkt `:` und werden an den Selektor angehängt.

<HtmlEditor
  htmlTransformer={(css) => `<style>${css}</style><body>Fahre mit der Maus über den <a href="#">👉 Link</a>.</body>`}
  hideWarning
  title='hover.css'
  lang='scss'
>
```css
a {
    color: blue;
    &:hover {
        color: orange;
    }
}
```
</HtmlEditor>

In diesem Beispiel wird die Schriftfarbe aller Links (`a`-Elemente) auf Blau gesetzt. Wenn ein Link mit der Maus berührt wird (`:hover`), ändert sich die Schriftfarbe zu Orange.

::::tip[Verschachtelte Regeln: `&`]
:::flex
Das `&`-Zeichen im obigen Beispiel steht für eine **Ergänzung des aktuellen Selektors**, in diesem Fall also `a`.  

Das obige Beispiel könnte also auch wie folgt geschrieben werden:
::br
```css
a {
    color: blue;
}
a:hover {
    color: orange;
}
```
:::
::::

Web-Dokumente ermöglichen, im Unterschied zu gedruckten Dokumenten, **Intergaktionen** mit den Betrachter:innen.

## Beispiele

export const example1 = `<h3>Meine Qualifikationen</h3>
<ul>
    <li>HTML</li>
    <li>CSS</li>
    <li>JavaScript</li>
    <li>Markdown</li>
    <li>React</li>
    <li>TypeScript</li>
</ul>`;

<CodeBlock language="html" showLineNumbers>
    {example1}
</CodeBlock>

::::flex{columns=3}
<HtmlEditor
  htmlTransformer={(css) => `<style>${css}</style><body>${example1}</body>`}
  hideWarning
  title='styles-1.css'
  lang='scss'
  minLines={13}
>
```scss
li {
    color: darkblue;
    &:hover {
        background-color: lightgray;
    }
}
```
</HtmlEditor>
:::info
Mit `:hover` wird der Hintergrund des Listenelements auf `lightgray` gesetzt, wenn die Maus über das Element fährt.
:::

::br
<HtmlEditor
  htmlTransformer={(css) => `<style>li { transform-origin: left; }${css}</style><body>${example1}</body>`}
  hideWarning
  title='styles-2.css'
  lang='scss'
  minLines={13}
>
```scss
li {
    color: darkblue;
    transition-duration: 0.5s;
    &:hover {
        color: orange;
        list-style-type: square;
        transform: scale(1.2);
    }
}
```
</HtmlEditor>

:::info
Mit `:hover` wird das entsprechende Listenelement
- orange eingefärbt :mdi[square]{.orange size=0.6}
- das Listensymbol auf ein Quadrat (`square`) gesetzt
- alles um den Faktor 1.2 vergrössert.

Damit die Änderung nicht abrupt geschieht, wird mit `transition-duration` die Übergangszeit festgelegt.
:::
::br
<HtmlEditor
  htmlTransformer={(css) => `<style>${css}</style><body>${example1}</body>`}
  hideWarning
  title='styles-3.css'
  lang='scss'
  minLines={13}
>
```scss
li {
    color: darkblue;
    &:first-child {
        color: orange;
    }
    &:nth-child(2n) {
        background-color: whitesmoke;
    }
    &:last-child {
        color: green;
    }
}
```
</HtmlEditor>
:::info
- Die Standardfarbe für Listenelemente ist :mdi[square]{color="darkblue" size=0.6} `darkblue`.
- Das erste Element (`:first-child`) wird in :mdi[square]{color="orange" size=0.6} `orange` dargestellt.
- Jedes zweite Element (`:nth-child(2n)`) bekommt einen Hintergrund von `whitesmoke`.
- Das letzte Element (`:last-child`) wird in :mdi[square]{color="green" size=0.6} `grün` dargestellt.
:::
::::

## Aufgaben

::::aufgabe[Pseudoklassen anwenden]
Ergänzen Sie die CSS-Regeln, so dass folgende Anforderungen erfüllt werden:

<ProgressState id="9b1f1d57-3584-4d83-b1c3-eb808d66aa39" keepPreviousStepsOpen>
1. Das erste `div`-Element soll einen roten Hintergrund erhalten.
2. Das letzte `div`-Element soll einen grünen Hintergrund erhalten.
3. Wird ein `div`-Element mit der Maus berührt, soll
    - der `border-radius` auf 50 % gesetzt werden,
    - die Hintergrundfarbe auf Hellblau geändert werden
    - die Grösse um den Faktor $1.5$ vergrössert werden.
4. Können Sie die Regeln so anordnen, dass die Hintergrundfarben des ersten und letzten Elements nicht ändern, wenn diese mit der Maus berührt werden?
</ProgressState>

:::info[Demo der Lösung]
<GridDemo />
:::

<HtmlEditor
  id='76768023-e4a7-4680-8714-626a2cb8681a'
  hideWarning
  title='aufgabe.html'
  lang='html'
  code={GridSkeleton}
  maxLines={33}
/>

<Solution id="4604dc75-81f3-4fe1-b544-8d8ebf2e8229">
    <CodeBlock language="html" showLineNumbers children={Grid} />
</Solution>
::::

:::aufgabe[Steckbrief fertigstellen]
Stellen Sie den [Steckbrief](../01-tag-selektoren/index.mdx#tag-selektoren-und-stilregeln) aus dem vorherigen Kapitel fertig, bevor Sie hier fortfahren.
:::

:::aufgabe[⭐ Bonusaufgabe: Steckbrief individualisieren]
<Answer type="state" id="f96e5aac-9ca4-4595-a9ae-9d55310d3a9a" />

1. Individualisieren Sie den Steckbrief des vorherigen Kapitels inhaltlich auf Ihre Person (Steckbrief unten eingefügt). Das Bild können Sie vorerst als Platzhalter beibehalten und dann später durch ein eigenes Bild ersetzen.
2. Fügen Sie mit CSS-Pseudoklassen interaktive Effekte hinzu, z.B.:
    - Ändern der Hintergrundfarbe, wenn die Maus über den Steckbrief fährt.
    - Vergrössern des Bildes, wenn die Maus darüber fährt.
    - Ändern der Schriftfarbe der Überschriften, wenn diese mit der Maus berührt werden.
3. Laden Sie den fertigen Steckbrief herunter und fügen Sie nun ein persönliches Bild ein (lesen Sie im Kapitel "HTML Links" nach, wie Sie lokale Bilder einbinden können).

<HtmlEditor hideWarning minHeight={'800px'} code={CvHtml} id="0d6a39d8-21f2-48e1-a7e0-d88501a9bc7b" />
:::